# Nexus Core 환경변수 설정 (.env)

# --- 데이터베이스 접속 URL (둘 중 하나만 활성화) ---
# [로컬 개발] Docker Compose 내부 DB
DATABASE_URL=postgresql+asyncpg://nexus_user:nexus_pass@db:5432/nexus_core
# [프로덕션] Supabase (비밀번호의 특수문자는 URL 인코딩 필요. 예: ! → %21)
# DATABASE_URL=postgresql+asyncpg://postgres:[YOUR-PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres?options=-csearch_path=nexus_core

# --- AI API 키 ---
GEMINI_API_KEY=your-gemini-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# --- 앱 설정 ---
DEBUG=true
UPLOAD_DIR=./uploads
CORS_ORIGINS=["http://localhost:3000"]
