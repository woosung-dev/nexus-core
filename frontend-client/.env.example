# Clerk Auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# 커스텀 로그인/회원가입 페이지 경로
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/signup

# FastAPI 백엔드 API 기본 주소 (로컬 개발용)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# 운영 도메인 (Open Graph/Twitter 카드의 absolute URL, sitemap, robots 에 사용).
# 로컬에선 fallback 으로 http://localhost:3000 사용. Vercel 운영에선 실제 도메인 입력.
NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app

# 하나로 SSO 공직자 판별 API v2 (서버 전용 — 절대 NEXT_PUBLIC_ 접두사 금지, 브라우저 노출 금지)
# IT팀이 별도 채널로 전달하는 v2 발급 키. 소스/저장소에 커밋 금지.
# 값은 = 뒤에 공백 없이 바로 붙인다. 키에 #·공백이 있으면 따옴표로 감쌀 것: OFFICIAL_CHECK_KEY="a#b"
# 키는 버전 전용이다. v1 키를 v2 주소에 쓰면 업스트림이 invalid_key 를 반환하고
# 라우트는 500(server_config)으로 실패한다(서버 로그에 업스트림 에러코드가 남는다).
# 미설정 시엔 하나로 API를 호출조차 하지 않고 즉시 같은 500 으로 실패한다.
OFFICIAL_CHECK_KEY=
OFFICIAL_CHECK_URL=https://hanaro.ffwp.or.kr/API_kim/officialLoginCheck2
