# 다음 세션 — 병목은 검색이다 (2026-08-15 작성 · **2026-08-17 정정**)

> ⛔ **처음 이 문서는 「게이트 과잉 87건이 본론」이라고 적었다. 그 결론은 틀렸다.**
> 자체 거절 234건을 원문과 함께 열어 보니 병목은 **검색**이었다. §3 앞의 정정을 먼저 읽어라.

> 앞 문서: `next-session-replay-first-2026-08-14.md` (STEP 3 replay 지시서)
> 이 문서는 **replay 를 다 돌린 뒤** 무엇이 남았는지 적는다.

---

## 0. 이번 세션에서 끝난 것 — 다시 하지 마라

| | 상태 |
|---|---|
| STEP 3 replay 600건 | ✅ **완료.** 오류 0 · 429 0 · `_e2e_replay_0815.json` |
| triage 자 | ✅ `exports/replay_2026-08/_triage.py` (LLM 호출 0회) |
| 결손 목록 | ✅ `exports/replay_2026-08/FINDINGS.md` (290줄 · 3섹션) |
| 관리자 3차 요청서 2순위 | ✅ 8주제 ○× 표 추가 · **미발송** |
| 하네스 `--no-backfill` | ✅ 턴당 호출 5.65 → 1회 |
| 인용 형식 지시 판정 | ✅ `decision-cite-instruction-2026-08-14.md` |

### ⚠ `exports/` 는 git 에 안 남는다

`.gitignore:25` 가 `/exports` 를 통째로 뺀다. 위 산출물과 스크립트는 **이 머신에만 있다.**
git 에 남은 것은 `docs/architecture/` 뿐이다. 재생성이 필요하면:

```bash
cd backend && .venv/bin/python ../exports/replay_2026-08/_triage.py --tag replay_0815 --md
```

---

## 1. 라이브 현재 상태 — 먼저 확인하고 시작할 것

```
봇 11   테스트 봇 D-1 ver2 · lexical · strict · gemini-3.5-flash-lite · is_active=t
        프롬프트 1,481자 (md5 86ffe8b8f9c084d390764b63f867039c) ← 인용 지시 포함
봇 1~10  is_active=false
ops_facts  1행 (crisis)
```

**로컬 봇 29 도 같은 1,481자다.** 한쪽만 바꾸지 마라.

```bash
LIVE=$(sed -n '11p' backend/.env | sed -E 's/^#[[:space:]]*DATABASE_URL=//' | tr -d '\r' \
       | sed 's/+asyncpg//; s/ssl=require/sslmode=require/')
docker exec -i nexus_db psql "$LIVE" \
  -c "select id,name,retrieval_mode,evidence_policy_mode,is_active,length(system_prompt) from bots order by id;"
```

### Gemini 키가 셋이다

| 키 | 어디 | 쓸 곳 |
|---|---|---|
| `…ufUU` | 프로덕션(Cloud Run) | **바꾸지 마라.** 봇 2~10 File Search 스토어가 묶여 있다 |
| `…F4lg` | 옛 로컬 `.env` | 2026-08-14 소진됨 |
| `…aXbI` | **현재 로컬 `.env`** | 측정용 |

**무료 한도는 키(프로젝트)마다 따로 500회/일**(리셋 KST 16:00)다. 어휘 경로는 스토어와 무관해
키를 바꿔도 안전하다 — replay 600건을 두 키로 나눠 돌려 실증했다(폴백 0건이라 조건 동일).

---

## 2. replay 결과 — 이번 세션의 본체

라이브 실사용자 발화 2,271건 → 고유 1,183건 → **2회 이상 반복 600건**을 로컬 봇 29 로 재질의.

| | 질문 | 원발화 |
|---|---:|---:|
| 전체 | 600 | 1,670 |
| **답 못 함** | **373 (62.2%)** | **1,050 (62.9%)** |

45문항 벤치마크의 유보 68.2% 와 실사용자 62.2% 가 근접했다 —
**벤치마크가 실사용자를 잘 대표했다.** 지금까지의 측정을 믿어도 된다.

### 답 못 한 373건의 구성 — **누가 막았나로 가른 것이 옳다** (2026-08-17 확정)

```
234건 (원발화 659)  봇이 스스로 거절     ← 게이트와 무관. 본체다
 69건 (원발화 204)  게이트: 지어냄       ← 게이트가 옳게 막음
 41건 (원발화 101)  게이트: 표기없음+지어냄
 29건 (원발화  86)  게이트: 표기 누락만   ← 상한 +4.8%p
```

> `_triage.py` 의 `문서없음/검색못함/미분류` 분류는 **어휘 기준**이라 이것과 축이 다르다.
> 자료 요청 후보를 고를 때만 쓰고, **「무엇을 고칠까」는 위 표로 판단하라.**

### ⚠⚠ 「문서없음 후보」는 문서 결손이 아니었다 — 자료 요청서를 보내기 직전에 잡았다

**167건 전부가 자료를 3건 이상 받았다.** 「검색이 빈손」은 0건이다.
그중 97건(원발화 284회)은 자료를 쥐고도 **표기 누락으로 게이트가 막았다.**

자가 놓친 것: **「질문의 말이 코퍼스에 있나」만 봤지 「받은 자료가 답이 되나」는 안 봤다.**

실증 — 「야동을 어떻게 생각하나요?」(4명):

| | |
|---|---|
| 코퍼스에 `야동` | 없음 → 자는 「문서없음」이라 판정 |
| 실제 | **제65·66조가 「음란물」로 이 쟁점을 정면으로 다룬다** |
| 같은 주제 다른 질문 | 「음란물 중독은 어떻게 해결」(2명)은 reg-65·66 이 주입돼 **제대로 답했다** |

즉 **자료 결손이 아니라 용어 불일치**다. 확인된 대응 11쌍:

```
야동·성인영상→음란물   불륜·외도→탈선     남편·아내→배우자    연애·데이트→교류
비용·돈→축복헌금       이혼→축복정리      아이→자녀          친구→식구
속궁합→부부관계        성관계→성별        당감봉→탕감봉(오타)
```

**원문을 한 글자씩 대조해 「정말 없는 것」은 두 개뿐이었다** — 축복반지를 **어느 손가락**에 끼는지
(7명이 물음, 「반지」는 제72·90조에 있으나 손가락은 어디에도 없음)와 **담배·흡연**(「술」은 5조문에 있음).

> **교훈: 어휘가 MISS 라고 자료 결손이 아니다.** 관리자에게 올리기 전에 **주입된 유닛 원문을
> 반드시 열어 봐라.** 이번에 안 열었으면 「성 관련 자료가 없습니다」를 보낼 뻔했다 — 규정집은
> 제65조 ⑧에서 **「세부 행위의 방법을 기준표처럼 설명하지 않는다」**고 이미 정해 두었다.

### 그래서 트랙은 이렇게 갈린다 — **섞지 마라**

| 트랙 | 크기 | 고치는 사람 | 우선순위 |
|---|---|---|---|
| ★★★ **검색** — 엉뚱한 조문을 물어다 준다 | 자체 거절 234건의 상당수 | 우리 | **1순위** (아래 정정) |
| ★ 게이트 표기 누락 | 29건 (86회) · 상한 +4.8%p | 우리 | 낮다 |
| ★ 검색 동의어 10쌍 | 38건 · 상한 +6.4%p, 28건이 게이트와 겹침 | 우리 | 곁가지 |
| 상담 영역 | 25건 (72회) | 응대 문구 | — |
| 진짜 자료 결손 | **2건** | 관리자 (반지 손가락 · 담배) | — |

---

## ⛔ 2026-08-17 정정 — 이 문서 §3 의 결론은 틀렸다. 병목은 게이트가 아니라 **검색**이다

**아래 §3 을 그대로 따르지 마라.** 답 못 한 373건을 다시 갈랐더니 그림이 완전히 달랐다.

```
234건 (원발화 659)  봇이 스스로 거절     ← 게이트와 무관. 여기가 본체다
 69건 (원발화 204)  게이트: 지어냄       ← 게이트가 옳게 막음
 41건 (원발화 101)  게이트: 표기없음+지어냄
 29건 (원발화  86)  게이트: 표기 누락만   ← 상한 +4.8%p 뿐이다
```

**왜 87건이 199건이 됐다가 29건이 됐나** — `_kpi.replay()` 의 `why` 는 **게이트가 막았는지와
무관하게** 계산된다. 봇이 스스로 거절한 턴도 근거를 안 짚었으므로 「①표기 누락」으로 라벨된다.
`blocked` 와 `sr` 을 갈라야 진짜 숫자가 나온다(`_inspect.py --why gate|self`).

### 진짜 병목 — 검색이 엉뚱한 조문을 물어다 준다

자체 거절 234건의 상위 26건을 **주입된 조문 제목과 함께** 눈으로 봤다.

| 사용자 질문 | 검색이 뽑아 준 것 | 코퍼스에 있는 정답 조문 |
|---|---|---|
| 축복 비용이 얼마인가 (5명) | 납부확인서 · **성화신고** | **제53조** 축복감사헌금 |
| 상대자가 불륜을 한 것 같다 (5명) | **제91조 자살 사안** · 미혼영인축복 | **제63~66조** 탈선 |
| BLESSING 4U 링크 (4명) | 행정지원센터 하나로 · **성물 관리** | **제25조** B4U 운영 기준 |
| 교류 중 다른 사람에게 마음이 갔다 (5명) | 가정코드 · **하늘공관금** · 탕감봉 의식 | — |
| 아이돌 팬심 (5명) | 참부모님 말씀 원문 반영 원칙 · 성화자 영인축복식 | — |

**「불륜」에 자살 조문이, 「비용」에 성화신고가 온다.** 코퍼스에 답이 있는데 못 뽑는다.

### ⚠ 어휘 지표로는 이걸 못 잡는다 — 이 세션에서 네 번 틀렸다

| | 지표로 본 것 | 원문·제목을 눈으로 보니 |
|---|---|---|
| 1차 | 자료 결손 144건 | 167건 전부 자료를 받았음 |
| 2차 | 게이트 과잉 199건 | 게이트는 29건, 234건은 자체 거절 |
| 3차 | 검색 관련도 86% — 괜찮음 | 「축복」 같은 흔한 말만 겹쳐도 hit 으로 센 **무딘 지표** |
| 4차 | — | **조문 제목을 보니 명백히 엉뚱** |

**매번 지표를 정교하게 만들려다 틀렸고, 원문을 열 때마다 맞았다. 열어라.**

### 다음 — 의미 검색(dense)이 통째로 빠져 있다

`store.py` 의 `WIKI_DENSE_SCALES` 기본값이 빈 문자열이라 **BM25 단독**이다.
뺀 이유는 `handoff-wiki-retrieval-2026-08-08.md` §5-1 에 있다 —
**「의미 검색은 file_search 가 이미 하는 일이라 우리가 만들면 복제다」.**

**그 전제가 지금은 없다. 라이브 봇 11 은 `lexical` 단독이라 file_search 를 안 탄다.**
즉 의미 검색이 파이프라인 어디에도 없다. 되돌리는 것은 환경변수 한 줄이다(같은 문서 §5-1).

**최소 측정** — 검색만 재면 생성 호출이 0회다. 유닛 임베딩 250(1회성) + 질문 임베딩 15 = **265회**.
모델 변동성이 안 섞여 판정이 결정적이다. 도구: `exports/wiki_eval/_probe.py` (dense/BM25/hybrid 나란히).

> ⚠ 임베딩 무료 한도는 **분당 100회 · 하루 1,000회**이고 생성 한도와 별개다.
> 사실문장 971개를 dense 로 넣으면 그날 질문 임베딩조차 못 쓴다 — `unit` 스케일만 켜라.

---

## 3. ~~표기 누락 87건~~ (원발화 242회) — **위 정정을 먼저 읽어라**

**답 못 한 것의 23%가 문서 문제도 검색 문제도 아니다.**
자료는 제대로 뽑혔는데(`unit_refs` 채워짐) 모델이 본문에 `(근거: …)` 표기를 안 해서 게이트가 막았다.

> ⚠ **「과잉 거절」이라고 단정하지 마라.** §5 의 새 정의로 45문항을 재면 **③ 은 1.5%(1/66)** 로
> 목표를 달성한 상태다. 그 분모는 「정답지가 **답이 문서에 있다**고 판정한 문항」인데,
> **replay 에는 정답지가 없어 그 분모를 못 만든다.** 87건 중에는 애초에 답이 없어 막힌 게 옳은
> 것도 섞여 있다.
>
> **먼저 아래 「재 볼 것」으로 답변을 눈으로 봐라.** 쓸 만한 답이 막혔으면 손해고,
> 쓸 만하지 않으면 게이트가 옳다. **재 보기 전에는 87건을 「손해」로 세지 마라.**

### 프롬프트로는 안 된다 — 이미 해봤다

`dc2affe` 가 인용 형식 지시를 넣어 **45문항에서 표기 누락 7건 → 1건**으로 잡았다.
그런데 **이 replay 는 그 1,481자 프롬프트로 돌았는데도 87건이다.**

이유가 보인다. 45문항은 조문을 묻는 규정 질문 위주인데, 실사용자는 이런 걸 묻는다.

```
14명  축복은 왜 받아야 해요?
12명  축복가기까지의 과정을 자세히 알려줘
 7명  축복을 정리하는 과정은 많이 까다로운가요?
```

**조문을 인용할 자연스러운 자리가 없는 열린 질문**이다. 프롬프트를 아무리 조여도
모델이 「제41조에 따르면 축복은 왜 받아야 하냐면」이라고 쓰게 만들 수는 없다.

> **프롬프트 5종째를 만들지 마라.** 앞 문서 「하지 말 것」 1번이고, 이번에 근거가 하나 더 생겼다.

### 볼 곳은 게이트다 — 「인용 표기가 세 겹」이 열쇠다

`dc2affe` 가 남긴 사실(`next-session-replay-first-2026-08-14.md` §2):

| 기전 | 어디서 오나 | 게이트가 읽나 |
|---|---|---|
| 자료 카드(문서명·스니펫·형광펜) | `wiki/service.py:_citations()` — 주입 유닛마다 자동 | ❌ |
| 본문 각주 `[1]` | `citationMarkers.ts` — 배경 백필이 채움 | ❌ |
| 본문 `(근거: 제41조)` | 모델이 직접 씀 | ✅ **이것만** |

셋째만 읽는 이유는 자료 카드가 **주입만 하면 무조건 생겨서** 「모델이 그 원문을 실제로 썼는가」를
증명하지 못하기 때문이다(#59 가 고친 함정).

**그래서 다음 질문은 이것이다: 본문 표기를 요구하지 않고 근거 사용을 검증할 수 있나?**

> ⚠ **「어휘 매칭으로 판정」은 3전 3패다.** 답변과 주입 원문의 단어 겹침으로 판정하려던 시도가
> 세 번 실패했다. 손대기 전에 그 이력부터 읽어라. 이번 세션의 `_triage.py` 도 같은 벽을 만났다
> (자세한 것은 그 파일 docstring 「어휘로 가를 수 있는 것에는 한계가 있다」).

### 먼저 재 볼 것 — 호출 0회

87건이 정말 「답할 수 있었는데 막힌」 것인지 확인해야 한다. legacy 로 돌았으므로
**모델의 실제 답변이 남아 있다.**

```bash
python3 -c "
import json
d=json.load(open('exports/replay_2026-08/_triage_replay_0815.json'))
for r in d['rows']:
    if r['triage']=='미분류' and r['gate_why'].startswith('①'):
        print(r['k'], r['q'][:50]); print('  ', r['answer_head'][:150])
" | head -60
```

답변이 실제로 쓸 만한지 눈으로 본다. 쓸 만하면 게이트가 손해를 내고 있는 것이고,
쓸 만하지 않으면 게이트가 옳다.

---

## 3-2. 검색 동의어 — 관리자 ○× 를 받으면 바로 넣는다

§2 에서 확인된 11쌍이다. **새 문서 없이 답변률이 오르는 부분**이라 투입 대비 효과가 가장 크다.

```
야동·성인영상→음란물   불륜·외도→탈선     남편·아내→배우자    연애·데이트→교류
비용·돈→축복헌금       이혼→축복정리      아이→자녀          친구→식구
속궁합→부부관계        성관계→성별        당감봉→탕감봉(오타)
```

**어디에 넣나** — 아직 안 정했다. 후보:
`wiki/retrieval.py:tokenize()` 에서 질의어를 확장하거나, 용어집(glossary) 트랙과 합치거나.
BM25 는 한글 bigram 을 이미 만들므로(`retrieval.py:56-57`) 확장이 과하면 정밀도가 떨어질 수 있다.
**넣기 전에 11쌍만으로 replay 재측정해서 답변률 변화를 재라** — 태그를 새로 쓰면 된다.

---

## 4. 관리자 3차 요청서 — 「자료 요청」에서 「용어 확인」으로 바뀌었다

`exports/admin_request_2026-08-14/요청서_3차_2026-08-14.md` · **미발송**

**처음에는 「규정 결손 144건을 주제 8개로」 썼다가 전부 걷어냈다.** §2 의 ⚠ 가 이유다 —
주입된 유닛 원문을 열어 보니 대부분 다른 말로 다루고 있었다. 지금 2순위는 이렇다.

| | 내용 | 관리자가 할 일 |
|---|---|---|
| 2순위 | **용어 대응 11쌍이 맞는지** | ○× 11줄 |
| 2순위 (이어서) | **정말 없는 것 2건** (반지 손가락 7명 · 담배 5명) | ○× 2줄 |
| 2순위 (이어서) | 45문항 검증에서 나온 8건 (기존) | ○× |

`ⓓ 표기 통일`과 겹쳐 보이지 않게 문서에 구분을 적어 뒀다 —
**ⓓ 는 챗봇이 답할 때 쓸 말, 2순위는 사용자가 물어볼 때 쓰는 말**이다.

> **임의로 발송하지 마라. 사용자가 보낸다.**

### `FINDINGS.md` 의 「판정」 열은 아직 비어 있다

자동 분류를 그대로 믿지 마라. §2 의 ⚠ 가 그 증거다.
**「코퍼스에 없는 말」이 곧 「자료가 없다」가 아니다** — 주입된 유닛 원문을 열어야 한다.

---

## 5. KPI — 기준 충돌은 해소됐고, 새 지표가 문제를 드러냈다

**2026-08-15 결정: `③ 과잉 거절` 에서 「지어냄」을 뺀다. 지어냄은 `④` 로 따로 센다.**
게이트가 옳게 막은 것을 「과잉」이라 부르는 게 이름이 틀린 것이었다. 섞으면 ③ 이 **구조적으로 도달
불가능**해진다(지어냄만 15%를 넘는다). `_kpi.py` 에 반영했다.

| 지표 | 목표 | 반영 전 | **라이브 현행** |
|---|---|---:|---:|
| ① 유해 확답 | 0건 | 0건 | **0건** ✅ |
| ② 유보율(문서없음) | ≥95% | 87.5% | 68.8% ❌ |
| ③ 과잉 거절 (지어냄 제외) | ≤5% | 10.6% | **1.5%** ✅ |
| ④ **지어냄률** (신설) | **미정** | 23.6% | **20.4%** |

### ⚠ ④ 가 다음 우선순위 후보다 — ③ 보다 크다

**5턴 중 1턴이 주입 밖 근거를 댄다.** 지금은 게이트가 막아 ① 이 0건이지만,
**게이트가 없으면 그대로 유해 확답이 된다.** 게이트가 유일한 방어선이라는 뜻이다.

- ④ 의 **목표치를 아직 안 정했다.** 정하려면 사용자에게 물어라
- **`file_search` 경로는 여전히 게이트 무보호다**(앞 문서 §0-5 결함) — 봇 11 은 `lexical` 이라
  지금은 안 걸리지만, 폴백이 나면 그리로 간다. replay 600건에서는 폴백 0건이었다

### 남은 미결정 — `_SELF_REFUSAL_RE` 의 구멍 (문서화됨 · 미수정)

`app/services/unanswered.py:120-131` 이 이 문장을 못 잡는다.

```
본 규정집에 상세히 명시되어 있지 않아 본 답변에서 확정하기 어렵습니다.
```

`규정집에\s*(?:는\s*)?(?:없|…)` 이 「규정집에」 **바로 뒤**를 보는데 실물에는 「상세히」가 끼어 있다.
→ 봇이 제대로 유보한 턴이 「유보 안 함」으로 집계된다. **유보율이 실제보다 낮게 보고된다.**

고칠지는 사용자 결정이다(채점 기준을 건드리는 일이고, 넓히면 오탐이 는다).

---

## 6. 함정

1. **라이브(Neon)에 쓰지 마라.** `_run_e2e.py:47` 이 DSN 에 `neon.tech` 가 보이면 종료하지만
   `psql`·관리자 화면엔 그 보호가 없다.
2. **라이브 `bots` 를 psql 로 고치면 `updated_at` 이 안 움직인다.** SQLAlchemy 의
   `onupdate=func.now()`(`app/models/bot.py:65-71`)를 안 타기 때문이다. **변경 이력이 아무 데도
   안 남는다.** 2026-08-14 에 병렬 세션 둘이 이것 때문에 라이브 프롬프트를 5분간 어긋나게 만들었다
   (경위: `decision-cite-instruction-2026-08-14.md` §1).
3. **병렬 세션이 돌 수 있다.** 라이브를 고치기 **전에** `git log -3` 을 다시 봐라.
   세션 시작 때 본 것으로 믿지 마라 — 인계문서 본문도 세션 중에 바뀔 수 있다.
4. **호출이 문항 수의 5.65배다.** `_fill_evidence` 가 인용 청크마다 1회
   (`rag/evidence.py:107`, 실측 평균 인용 4.65개). `--no-backfill` 로 끄면 1회다.
   판정은 `trace` 로 하므로 결손 수집에는 영향이 없다. **`--throttle 10` 미만은 그래도 금지.**
5. **검색 점수로 결손을 판정하지 마라. 3전 3패다.**
6. **거대 유닛 함정.** `reg-100`(29,774자)·`reg-3`(8,377자)에는 아무 말이나 다 있다(중앙값 232자).
   「질문의 말이 reg-100 에 있다」는 아무 정보가 아니다.
7. **로컬 `bots.id=11` 은 `opus2_v4` 로 다른 봇이다.** D-1 ver2 는 **29번**.
8. **`unanswered._SELF_REFUSAL_RE` 와 `strict_mode._REFUSAL_RE` 는 다른 자다. 재사용 금지.**
9. **strict 차단 턴은 `unanswered_questions.reason` 에 `self_refusal` 로 들어간다** —
   `STRICT_EVIDENCE_MESSAGE` 자체가 그 정규식에 걸린다. 「게이트가 막음」과 「봇이 스스로 거절함」을
   그 테이블만으로는 못 가른다. 구분은 `messages.trace` 의 `STRICT` 단계로 한다.

---

## 7. 하지 말 것

- **프롬프트 5종째를 만들지 마라.** 인용 형식 지시가 표적을 맞혔는데도 실사용자 질문에서 다시
  커졌다. 남은 것은 프롬프트 문제가 아니다
- 파일럿을 다시 설계하지 마라 (사람이 없다)
- 비교군 봇을 만들지 마라
- 채점 기준을 또 바꾸지 마라 — 바꿔야 하면 **바꾸기 전에** 물어라
- 관리자 요청서를 임의로 발송하지 마라
- **`문서없음` 과 `검색못함` 과 `게이트 과잉` 을 섞지 마라.** 셋은 고치는 사람이 다르다

---

## 8. 정리 대상 (미처리)

- 로컬 DB 에 `e2e replay_0815` 세션 **600건** — 측정 부산물. 지울지는 판단 필요
- 라이브에 `[검증]` 제목 테스트 세션 6건 — 앞 문서에서 넘어온 것, 아직 안 지웠다
- 스트리밍 3갈래 미배선 · 예외로 죽은 턴 · 멀티턴 latch 없음 — 문서화됨, 미구현
