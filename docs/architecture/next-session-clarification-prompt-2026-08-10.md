# 다음 세션 프롬프트 — 맥락 부족 시 재질문 트리거 (2026-08-10)

새 세션을 열고 `/config` 에서 effort 를 **xhigh** 로 둔 뒤,
아래 블록 안쪽을 복사해 붙여넣는다.

```
docs/architecture/handoff-clarification-2026-08-10.md 를 먼저 읽어라.
목표·산출물·제약·열려 있는 선택지는 그 문서 §9 에 있고, 근거가 되는 사실은 §1~§8 에
확인 명령과 함께 있다.

문서를 믿기 전에 §0 의 표를 명령으로 대조하라. 값이 다르면 그 사이에 코드가 바뀐
것이고, 문서가 아니라 코드가 맞다.

레포는 /Users/woosung/project/agy-project/nexus-core, 브랜치 main 이다.
```

## 왜 이렇게 짧은가

내용을 줄인 게 아니라 **핸드오프 문서로 옮겼다.** 프롬프트에 제약을 복사해 두면
문서와 프롬프트가 따로 낡는다. 사실은 한 곳에만 둔다.

바꿀 게 생기면 `handoff-clarification-2026-08-10.md` §9 만 고치면 된다.
