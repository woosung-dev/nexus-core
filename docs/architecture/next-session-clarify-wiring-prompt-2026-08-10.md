# 다음 세션 프롬프트 — 되묻기 배선

아래 ---시작--- / ---끝--- 사이를 그대로 복사해서 새 세션에 붙여넣는다.

**선행 조건: `next-session-strict-gate-prompt-2026-08-10.md` 가 끝난 뒤에 시작한다.**
둘 다 답변 동작을 바꾸므로 같은 배포에 넣으면 안 된다.

---시작---

docs/architecture/handoff-clarify-wiring-2026-08-10.md 를 먼저 읽어라.
**§0 을 읽기 전에는 아무것도 시작하지 마라.**

봇이 되물어야 할 곳에서 안 되묻는다 — 실전 모호 5문항 중 0건이고, 같은 질문에도
결과가 흔들린다(§1). 판정기와 문구 생성기는 이미 다 있다(§2). 그런데 **규칙이 있는
봇이 0개라, 지금 배선하면 되묻는 대신 전부 「담당자에게 문의하세요」로 빠진다.**

그래서 순서가 전부다(§3):
① 오늘 실패한 5건(33·34·36·45·18)에 맞는 슬롯 규칙을 **사용자와 함께** 쓴다
   — 어떤 슬롯으로 답이 갈리는지는 규정 지식이라 혼자 정하지 마라
② 관리자 화면 「추가 확인 질문 정책 → 테스트하기」로 규칙이 걸리는지 먼저 확인한다
③ 그 다음이 배선이다
④ 발동률과 되물은 뒤 품질을 잰다

판정 축을 바꾸지 마라. `needs_user_input` 이지 `answerable` 이 아니다 — 후자는 45문항
중 35건(78%)에서 발동해서 못 쓴다. 나머지 제약은 §4, 함정은 §5.

문서를 믿기 전에 §0 과 §6 의 확인 명령을 돌려라. 값이 다르면 그 사이에 코드가 바뀐
것이고, 문서가 아니라 코드가 맞다.

레포는 /Users/woosung/project/agy-project/nexus-core,
브랜치는 main 을 최신으로 당긴 뒤 새로 딴다.

---끝---

## 크기

규칙 작성(사용자와 함께) 반나절 + 배선 반나절 + 프론트 1일 + 측정 반나절.
**프론트가 큰 몫이다** — 지금 클라이언트는 선택지를 안 읽는다(§4 마지막).

## 왜 strict 게이트 다음인가

게이트는 사용자에게 안 보인다. 되묻기는 봇이 답 대신 질문을 하기 시작하므로 바로 보인다.
되돌리기 쉬운 것부터 한다.

## 이 세션에서 사용자에게 물어야 할 것

**오늘 실패한 5건이 각각 어떤 슬롯으로 갈리는가.** 판정기가 짚은 후보가 §3-① 표에 있지만
그게 맞는지는 규정 지식이다. 이걸 틀리면 규칙 전체가 헛돈다 — 시드 규칙 4개가 그렇게 됐다.
