# LLM 위키 — 남은 일

> 2026-08-08 기준. 완료된 것은 `handoff-llm-wiki-2026-08-07.md` §8 의 0~4단계.
> 위에서부터 순서대로 하는 것을 전제로 쓴다 — 앞이 실패하면 뒤는 안 해도 된다.

## 지금 상태

```
raw 250건 → 위키 138쪽 · 문장 971개 · 모순 8건 · 공백 237건
인용 대조 1541/1545 (99.7%) · 형식 위반 0 · 소스 커버리지 250/250
ingest 250/250 · 실패 0 · 6.6시간 · 과금 0

백엔드에 wiki 참조 0건 — 챗봇 답변은 아직 아무 영향 없다
```

---

## P0 — 판정에 필요한 것

- [ ] **인용 불일치 4건 수리** — 앵커 오배정 2건(`glo-148`→`reg-26`, `reg-67`→`reg-54`),
      원문 축약 2건(`reg-44` 한 단어 축약, `reg-48` PDF 표 열 이어붙임).
      사실을 지어낸 것은 없다. `_verify.py` 는 지금 보고만 하고 수리하지 않는다.
- [ ] **모순 8건 검토** — 관리자가 판정할 만한 것인지 사람이 본다.
      쓸모없으면 이 로드맵 전체가 의미 없다. **이게 진짜 판정 기준이다.**
- [ ] **공백 237건 표본 검사** — 진짜 문서 공백인지, AI 가 질문을 과하게 만든 것인지.
      모순보다 27배 많아서 비율이 이상하다.

## P1 — 저장과 화면

- [ ] **DB 적재** (`wiki_pages` 외 4테이블, 마이그레이션 `down_revision = a1f6c30d84be`)
      지금은 파일 → TS 상수 직결이라 **관리자가 판정을 눌러도 저장할 곳이 없다.**
      읽기 전용 검수가 현재 구조의 천장이다.
- [ ] **임시 다리 2개 제거** — `_gen_admin.py`(파일→TS), `/api/wiki-file`(dev 전용 파일 서빙).
      DB 적재가 끝나면 둘 다 버린다.
- [ ] **`/ops-facts` 에 봇 선택기 부착** — 서버는 이미 `bot_id`·`scope` 를 지원하는데 화면이 안 쓴다.

## P2 — 챗봇 연결

- [ ] **위키 페이지를 Gemini 스토어에 봇 11 태그로 업로드** — 원문 조각 대신 큐레이션 페이지가
      검색 대상이 되게 한다.
- [ ] **미결: 원문을 함께 남길 것인가.** 남기면 청크가 두 종류가 되어 검색이 어느 쪽을 고를지
      통제하기 어렵고, 빼면 위키가 근거를 못 댈 때 폴백이 없다. 실측으로 정한다.
- [ ] **회귀 하네스로 개선 측정** — `exports/regression/` 55문항. 위키 연결 전후 비교.
      개선이 없으면 여기서 접는다.

## P3 — 갱신

- [ ] **원본 개정 시 갱신 경로** → `wiki-source-update-design-2026-08-08.md`
      관리자가 판정을 저장하기 시작하면 필수가 된다. 그 전에는 재실행으로 충분하다.

---

## 안 하기로 한 것

- **실서버(Cloud Run)에서 위키 생성** — 2026-08-08 결정.
  codex CLI 는 서버에서 못 쓰고(개인 구독 인증·컨테이너에 미설치),
  API 로 바꾸면 250호출 × 약 12,771 토큰 ≈ **320만 입력 토큰**의 과금이 매 재생성마다 발생한다.
  Cloud Run Jobs(최대 168시간)로 기술적으로는 가능하지만 **비용 때문에 접었다.**
  규정집 개정은 몇 달에 한 번이므로 로컬 생성으로 충분하다.
  - 참고로 필요했을 것: 생성 엔진 API 전환 · 툴 3개짜리 에이전트 루프 ·
    정본을 파일→DB 로 전환 · 봇별 동시 실행 락 · 진행률 UI

- **위키 산출물을 git 에 커밋** — 이 레포는 public 이고,
  규정집 v20 은 승인 전 개정초안("초안 조문번호의 대외 인용 금지"),
  대사전 v4 는 사용 승인 미결이다. `frontend-admin/src/features/llm-wiki/{wiki,sources}.ts` 는
  gitignore 한다. 받는 사람은 원본 PDF 2개로 `_gen_admin.py` 를 돌려 만든다.

---

## 알려진 결함

- **`## 요약` 을 아무도 검사하지 않는다.** 규약상 `## 사실` 의 압축이어야 하지만
  프로그램이 검증하지 않는다. 요약에만 있는 내용이 들어가도 안 잡힌다.
- **`_verify.py` 는 보고만 한다.** `golden_2026-08/_draft.py` 에는 자가수리(최대 2회)가 있는데
  위키 쪽에는 옮기지 않았다.
- **동시 실행 방지가 사람 손에 달려 있다.** 2026-08-08 에 `pgrep` 오탐으로
  실제 ingest 두 개가 3분간 동시에 돌았다. 락이 없다.
