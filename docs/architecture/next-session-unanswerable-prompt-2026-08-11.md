# 다음 세션 프롬프트 — 「답변 못 함」 3층 구조 (2026-08-11)

새 세션을 열고 `/config` 에서 effort 를 **xhigh** 로 둔 뒤,
아래 블록 **안쪽**을 복사해 붙여넣는다.

---시작---
```
docs/architecture/handoff-unanswerable-layer-2026-08-11.md 를 먼저 읽어라.
만들 것은 §2 의 3층(결정론 게이트 · 구조화 출력 · 기록과 관리자 화면)이고,
제약은 §3, 열려 있는 결정은 §4, 다음 세션이 모를 맥락은 §6 에 있다.

문서를 믿기 전에 §7 의 확인 명령을 돌려라. 값이 다르면 그 사이에 코드가 바뀐
것이고, 문서가 아니라 코드가 맞다.

관리자 화면은 /ui-ux-pro-max:ui-ux-pro-max 스킬을 써서 만들어라.

레포는 /Users/woosung/project/agy-project/nexus-core,
브랜치 feat/clarification-trigger 다.
```
---끝---

## 왜 이렇게 짧은가

내용을 줄인 게 아니라 **핸드오프 문서로 옮겼다.** 프롬프트에 제약을 복사해 두면
문서와 프롬프트가 따로 낡는다. 사실은 한 곳에만 둔다.

바꿀 게 생기면 `handoff-unanswerable-layer-2026-08-11.md` 만 고치면 된다.

## 먼저 물어봐야 할 것 (§4)

다음 세션은 구현을 시작하기 전에 이 셋을 사용자에게 확인해야 한다.

1. **문구** — 「아직 **학습**되지 않은」 vs 「아직 **정리**되지 않은」
2. **로그 접근 권한·보존 기간** (실제 사용자 질문이 관리자 화면에 그대로 뜬다)
3. **검수 주체·주기** (「아직 정리 안 됨」은 사용자에게 약속이 된다)
